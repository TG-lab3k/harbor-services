# Server
PORT=8080
BASE_URL=http://localhost:8080
DB_BACKEND=memory

# Admin (fail-closed when ADMIN_EMAILS empty)
ADMIN_APP_ID=harborAdmin
ADMIN_EMAILS=["admin@example.com"]
ADMIN_PASSWORD=AdminPass123
SEED_ON_START=true

# JWT
JWT_ISSUER=harbor-services
ACCESS_TOKEN_TTL=7200
REFRESH_TOKEN_TTL=2592000
# 生产必配（Secret Manager → RSA_PRIVATE_KEY_PEM）；kid 由公钥派生，同 PEM 跨重启不变
# 本地 DB_BACKEND=memory 时可省略（进程内临时密钥）
# RSA_PRIVATE_KEY_PEM=
# RSA_PUBLIC_KEY_PEM=

# Crypto
ENCRYPTION_KEY=dev-encryption-key-change-me!!
BCRYPT_COST=12
APP_CACHE_TTL_SEC=300

# Firestore (when DB_BACKEND=firestore)
# GCP_PROJECT_ID=local-project
# FIRESTORE_EMULATOR_HOST=localhost:8081
